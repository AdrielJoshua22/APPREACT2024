import React from 'react';
import './Spinner.css'; // Asegúrate de tener un archivo CSS llamado Spinner.css en la misma carpeta

const Spinner = () => {
  return (
    <div className="spinner"></div>
  );
};

export default Spinner;
